<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="RoclandAccesoControl.Mobile.Views.Popups.FotoRequeridaPopup"
               x:TypeArguments="x:Boolean"
               BackgroundColor="#FFFFFFFF"
               CanBeDismissedByTappingOutsideOfPopup="False">

    <Border BackgroundColor="#FFFFFF"
            StrokeShape="RoundRectangle 20,20,0,0"
            Stroke="Transparent"
            Padding="24,28,24,40">
        <Border.Shadow>
            <Shadow Brush="#000000" Offset="0,-4" Radius="20" Opacity="0.1"/>
        </Border.Shadow>

        <VerticalStackLayout Spacing="24">

            <!-- Ícono de advertencia -->
            <Border BackgroundColor="#FEF3C7"
                    StrokeShape="RoundRectangle 20"
                    Stroke="Transparent"
                    Padding="16"
                    HorizontalOptions="Center">
                <Image Source="icon_camera.png" HeightRequest="32" WidthRequest="32"/>
            </Border>

            <!-- Textos -->
            <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                <Label Text="Foto requerida"
                       FontSize="18" FontAttributes="Bold"
                       TextColor="#111827"
                       HorizontalOptions="Center"/>
                <Label Text="Esta persona no tiene foto de identificación."
                       FontSize="14" TextColor="#374151"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center"/>
                <Label Text="¿Desea capturarla ahora?"
                       FontSize="14" FontAttributes="Bold"
                       TextColor="#D97706"
                       HorizontalOptions="Center"
                       Margin="0,4,0,0"/>
            </VerticalStackLayout>

            <!-- Botones -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Border BackgroundColor="#F3F4F6"
                        StrokeShape="RoundRectangle 12"
                        Stroke="Transparent"
                        Padding="0,18">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCancelar"/>
                    </Border.GestureRecognizers>
                    <Label Text="Cancelar" FontSize="15" FontAttributes="Bold"
                           TextColor="#374151" HorizontalOptions="Center"/>
                </Border>

                <Border Grid.Column="1"
                        BackgroundColor="#D97706"
                        StrokeShape="RoundRectangle 12"
                        Stroke="Transparent"
                        Padding="0,18">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnTomarFoto"/>
                    </Border.GestureRecognizers>
                    <Border.Shadow>
                        <Shadow Brush="#D97706" Offset="0,4" Radius="12" Opacity="0.3"/>
                    </Border.Shadow>
                    <Label Text="Sí, tomar foto" FontSize="15" FontAttributes="Bold"
                           TextColor="White" HorizontalOptions="Center"/>
                </Border>
            </Grid>

        </VerticalStackLayout>
    </Border>
</toolkit:Popup>